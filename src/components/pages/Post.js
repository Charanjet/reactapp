const Post = props => {
    return (
        <div>
            id : {props.id}
            Title : {props.name}
            author : {props.name}
            body : {props.body}
            author : {props.name}
        </div>
    );
};

export default Post;